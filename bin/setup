#!/bin/bash

release=${1:-"tag"}

if [ "$release" == "tag" ]
  then
    git fetch
    tag=$(git describe --tags "$(git rev-list --tags --max-count=1)")
    git checkout "$tag"
fi

if [ "$release" != "tag" ]
  then
    git fetch
    git checkout "$release"
fi

yarn install --frozen-lockfile
yarn build
